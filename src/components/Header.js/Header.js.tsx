import React from 'react';


function Head(props) {
    return(
        <div>
            <header>
                <h1>{props.name}</h1>
            </header>
        </div>
    )
}

};

export default Head.js;
